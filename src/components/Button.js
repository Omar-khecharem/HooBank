import React from 'react'
import styles from '../Style'

const Button = () => (
  <button className={`bg-blue-gradient w-[150px] h-[60px] rounded-lg text-black font-semibold text-xl`}>
    Get Started
  </button>
)

export default Button